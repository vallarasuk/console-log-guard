name: Test Console Log Guard

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  check-logs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Console Log Guard
        uses: ./
        with:
          directory: 'src'
